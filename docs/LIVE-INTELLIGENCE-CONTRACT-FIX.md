# OLFACTUS v2.3.0-alpha.2 Live Intelligence Contract Repair

Corrects the remaining live-dashboard test and build failures:

- empty collection state is evaluated before rotation and diversity
- dashboard preference reads explicitly return DashboardPreferences
- customizer state preserves the density union
- legacy Today tests now validate LiveMissionControl
