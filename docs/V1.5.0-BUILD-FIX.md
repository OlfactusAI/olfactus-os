# OLFACTUS v1.5.0 Build Fix

Fixes:
- outdated v1.0.0 system manifest test
- optional `favorite` values in Genome and Profile
- implicit `any` in recommendation-path traversal
- invalid Neural Graph Search type predicate

The installer makes targeted in-place changes to the existing v1.5.0 project.
