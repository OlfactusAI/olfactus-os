import type { CollectionHealthAnalysis } from "@/lib/domain/analysis";
const labels={roleCoverage:"Role coverage",seasonalBalance:"Seasonal balance",diversity:"DNA diversity",redundancy:"Redundancy control",rotation:"Rotation health",intent:"Intent alignment",identity:"Identity strength"};
export function HealthDimensions({analysis}:{analysis:CollectionHealthAnalysis}){return <div className="grid gap-4">{Object.entries(analysis.dimensions).map(([key,value])=><div key={key}><div className="mb-2 flex justify-between gap-4 text-sm"><span className="text-[var(--muted)]">{labels[key as keyof typeof labels]}</span><strong>{value}</strong></div><div className="h-2 overflow-hidden rounded-full bg-[var(--surface-soft)]"><div className="h-full rounded-full bg-[linear-gradient(90deg,var(--gold),var(--amber))]" style={{width:`${value}%`}}/></div></div>)}</div>}
