import {
  DatasetReviewConsole,
} from "@/components/gold-standard-builder/dataset-review-console";

export default function GoldStandardDatasetReviewPage() {
  return (
    <main className="mx-auto w-full max-w-[1600px] px-4 py-6 md:px-6 lg:px-8">
      <DatasetReviewConsole />
    </main>
  );
}
